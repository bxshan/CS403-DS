public class node {
  private val;
  private ptr;

  public node(v, p) {
    val = v;
    ptr = p;
  }

  public getVal() {
    return val;
  }

  public getPtr() {
    return ptr;
  }

  public setVal(v) {
    val = v;
  }

  public setPtr(p) {
    ptr = p;
  }
}
